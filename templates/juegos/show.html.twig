{% extends 'base.html.twig' %}

{% block title %}Juegos{% endblock %}

{% block body %}
    <h1>Juegos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ juego.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ juego.nombre }}</td>
            </tr>
            <tr>
                <th>Activo</th>
                <td>{{ juego.activo ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Token</th>
                <td>{{ juego.token }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_juegos_index') }}">back to list</a>

    <a href="{{ path('app_juegos_edit', {'id': juego.id}) }}">edit</a>

    {{ include('juegos/_delete_form.html.twig') }}
{% endblock %}
