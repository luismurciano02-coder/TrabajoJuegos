{% extends 'base.html.twig' %}

{% block title %}Listado de Juegos{% endblock %}

{% block body %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb" style="background: rgba(255, 255, 255, 0.1); border-radius: 10px; padding: 10px;">
        <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Inicio</a></li>
        <li class="breadcrumb-item active" aria-current="page">Juegos</li>
    </ol>
</nav>
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="mb-0">🎯 Juegos Disponibles</h1>
            <p class="text-muted">Juegos filtrados por tu token. ¡Elige tu aventura!</p>
        </div>
        <a href="{{ path('app_juegos_new') }}" class="btn btn-success btn-lg">➕ Crear Nuevo Juego</a>
    </div>

    <div class="row">
        {% for juego in juegos %}
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">🎮 {{ juego.nombre }}</h5>
                        <p class="card-text flex-grow-1">
                            <strong>Activo:</strong> {{ juego.activo ? '✅ Sí' : '❌ No' }}<br>
                            <strong>Token:</strong> {{ juego.token }}
                        </p>
                        <div class="mt-auto">
                            <a href="{{ path('app_juegos_show', {'id': juego.id}) }}" class="btn btn-primary me-2">👀 Ver Detalles</a>
                            <a href="{{ path('app_juegos_edit', {'id': juego.id}) }}" class="btn btn-secondary">✏️ Editar</a>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-info" role="alert">
                    🚫 No se encontraron juegos para tu token. ¡Crea uno nuevo!
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
