{% extends 'base.html.twig' %}

{% block title %}Listado de Usuarios{% endblock %}

{% block body %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb" style="background: rgba(255, 255, 255, 0.1); border-radius: 10px; padding: 10px;">
        <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Inicio</a></li>
        <li class="breadcrumb-item active" aria-current="page">Usuarios</li>
    </ol>
</nav>
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="mb-0">👥 Usuarios Registrados</h1>
            <p class="text-muted">Gestiona los usuarios de la plataforma.</p>
        </div>
        <a href="{{ path('app_user_new') }}" class="btn btn-success btn-lg">➕ Crear Nuevo Usuario</a>
    </div>

    <div class="row">
        {% for user in users %}
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">👤 {{ user.nombre|default(user.email) }}</h5>
                        <p class="card-text flex-grow-1">
                            <strong>ID:</strong> {{ user.id }}<br>
                            <strong>Email:</strong> {{ user.email }}<br>
                            <strong>Roles:</strong> {{ user.roles ? user.roles|join(', ') : 'Ninguno' }}<br>
                            <strong>Token:</strong> {{ user.token }}<br>
                            <strong>Activo:</strong> {{ user.activo ? '✅ Sí' : '❌ No' }}
                        </p>
                        <div class="mt-auto">
                            <a href="{{ path('app_user_show', {'id': user.id}) }}" class="btn btn-primary me-2">👀 Ver Detalles</a>
                            <a href="{{ path('app_user_edit', {'id': user.id}) }}" class="btn btn-secondary">✏️ Editar</a>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-info" role="alert">
                    🚫 No se encontraron usuarios registrados.
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
