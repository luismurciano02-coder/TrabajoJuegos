{% extends 'base.html.twig' %}

{% block title %}Listado de Aplicaciones{% endblock %}

{% block body %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb" style="background: rgba(255, 255, 255, 0.1); border-radius: 10px; padding: 10px;">
        <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Inicio</a></li>
        <li class="breadcrumb-item active" aria-current="page">Aplicaciones</li>
    </ol>
</nav>
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="mb-0">📱 Aplicaciones Disponibles</h1>
            <p class="text-muted">Gestiona las aplicaciones de la plataforma.</p>
        </div>
        <a href="{{ path('app_aplicaciones_new') }}" class="btn btn-success btn-lg">➕ Crear Nueva Aplicación</a>
    </div>

    <div class="row">
        {% for aplicacione in aplicaciones %}
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">📱 {{ aplicacione.nombre }}</h5>
                        <p class="card-text flex-grow-1">
                            <strong>ID:</strong> {{ aplicacione.id }}<br>
                            <strong>Activo:</strong> {{ aplicacione.activo ? '✅ Sí' : '❌ No' }}<br>
                            <strong>API Key:</strong> {{ aplicacione.apikey }}
                        </p>
                        <div class="mt-auto">
                            <a href="{{ path('app_aplicaciones_show', {'id': aplicacione.id}) }}" class="btn btn-primary me-2">👀 Ver Detalles</a>
                            <a href="{{ path('app_aplicaciones_edit', {'id': aplicacione.id}) }}" class="btn btn-secondary">✏️ Editar</a>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-info" role="alert">
                    🚫 No se encontraron aplicaciones registradas.
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
