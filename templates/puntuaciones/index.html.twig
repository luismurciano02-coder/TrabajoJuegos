{% extends 'base.html.twig' %}

{% block title %}Listado de Puntuaciones{% endblock %}

{% block body %}
<nav aria-label="breadcrumb">
    <ol class="breadcrumb" style="background: rgba(255, 255, 255, 0.1); border-radius: 10px; padding: 10px;">
        <li class="breadcrumb-item"><a href="{{ path('app_home') }}">Inicio</a></li>
        <li class="breadcrumb-item active" aria-current="page">Puntuaciones</li>
    </ol>
</nav>
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <div>
            <h1 class="mb-0">⭐ Puntuaciones Registradas</h1>
            <p class="text-muted">Revisa las puntuaciones de los juegos.</p>
        </div>
        <a href="{{ path('app_puntuaciones_new') }}" class="btn btn-success btn-lg">➕ Crear Nueva Puntuación</a>
    </div>

    <div class="row">
        {% for puntuacione in puntuaciones %}
            <div class="col-md-4 mb-4">
                <div class="card h-100">
                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">⭐ Puntuación #{{ puntuacione.id }}</h5>
                        <p class="card-text flex-grow-1">
                            <strong>Puntuación:</strong> {{ puntuacione.puntuacion }}<br>
                            <strong>Fecha:</strong> {{ puntuacione.fecha ? puntuacione.fecha|date('d/m/Y H:i') : 'N/A' }}
                        </p>
                        <div class="mt-auto">
                            <a href="{{ path('app_puntuaciones_show', {'id': puntuacione.id}) }}" class="btn btn-primary me-2">👀 Ver Detalles</a>
                            <a href="{{ path('app_puntuaciones_edit', {'id': puntuacione.id}) }}" class="btn btn-secondary">✏️ Editar</a>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <div class="col-12">
                <div class="alert alert-info" role="alert">
                    🚫 No se encontraron puntuaciones registradas.
                </div>
            </div>
        {% endfor %}
    </div>
</div>
{% endblock %}
