<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}GameCenter{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>🎮</text></svg>">
        <link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&display=swap" rel="stylesheet">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" crossorigin="anonymous">
            <style>
                body {
                    background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
                    color: #e0e0e0;
                    font-family: 'Press Start 2P', cursive;
                    min-height: 100vh;
                    padding-top: 20px;
                    padding-bottom: 20px;
                }
                .container {
                    padding-left: 2rem;
                    padding-right: 2rem;
                }
                .card {
                    background: rgba(255, 255, 255, 0.1);
                    border: 2px solid #00d4ff;
                    border-radius: 15px;
                    box-shadow: 0 0 20px rgba(0, 212, 255, 0.3);
                    margin-bottom: 2rem;
                    padding: 1.5rem;
                }
                .card-header {
                    background: rgba(0, 212, 255, 0.2);
                    border-bottom: 2px solid #00d4ff;
                    color: #00d4ff;
                }
                .btn-primary {
                    background: linear-gradient(45deg, #ff6b6b, #ffa500);
                    border: none;
                    color: white;
                    font-weight: bold;
                    transition: all 0.3s ease;
                }
                .btn-primary:hover {
                    background: linear-gradient(45deg, #ffa500, #ff6b6b);
                    box-shadow: 0 0 15px rgba(255, 107, 107, 0.5);
                    transform: scale(1.05);
                }
                .btn-secondary {
                    background: linear-gradient(45deg, #4ecdc4, #44a08d);
                    border: none;
                    color: white;
                    font-weight: bold;
                    transition: all 0.3s ease;
                }
                .btn-secondary:hover {
                    background: linear-gradient(45deg, #44a08d, #4ecdc4);
                    box-shadow: 0 0 15px rgba(78, 205, 196, 0.5);
                    transform: scale(1.05);
                }
                .btn-success {
                    background: linear-gradient(45deg, #00d4ff, #0099cc);
                    border: none;
                    color: white;
                    font-weight: bold;
                    transition: all 0.3s ease;
                }
                .btn-success:hover {
                    background: linear-gradient(45deg, #0099cc, #00d4ff);
                    box-shadow: 0 0 15px rgba(0, 212, 255, 0.5);
                    transform: scale(1.05);
                }
                .display-4 {
                    color: #00d4ff;
                    text-shadow: 0 0 10px rgba(0, 212, 255, 0.5);
                }
                .lead {
                    color: #e0e0e0;
                }
                .form-control {
                    background: rgba(255, 255, 255, 0.1);
                    border: 2px solid #00d4ff;
                    color: #000000;
                }
                .form-control:focus {
                    background: rgba(255, 255, 255, 0.2);
                    border-color: #ff6b6b;
                    box-shadow: 0 0 10px rgba(255, 107, 107, 0.5);
                    color: #000000;
                }
                .form-label {
                    color: #00d4ff; /* Un azul cian que combina con el tema */
                    font-weight: bold;
                    margin-bottom: 0.5rem;
                    font-size: 0.9rem; /* Un poco más pequeño para que no sea tan grande con la fuente Press Start 2P */
                }
                .card .card-text, .card p, .card li {
                    color: #ffffff;
                }
                .alert {
                    border-radius: 10px;
                    color: #000000;
                }
                .breadcrumb {
                    background: transparent;
                    border-radius: 10px;
                }
                .breadcrumb-item a {
                    color: #4ecdc4;
                }
                .breadcrumb-item.active {
                    color: #00d4ff;
                }
                .card-title {
                    color: #00d4ff;
                }
                .text-muted {
                    color: #cccccc !important;
                }
                a {
                    color: #4ecdc4;
                }
                a:hover {
                    color: #00d4ff;
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" crossorigin="anonymous"></script>
        {% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark" style="background: rgba(0, 0, 0, 0.8); border-bottom: 2px solid #00d4ff; font-size: 0.9rem; padding: 1rem 2rem;">
            <div class="container-fluid">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav me-auto">
                        <li class="nav-item me-4">
                            <a class="nav-link fw-semibold" href="{{ path('app_home') }}">🏠 Inicio</a>
                        </li>
                        <li class="nav-item me-4">
                            <a class="nav-link fw-semibold" href="{{ path('app_juegos_index') }}">🎯 Juegos</a>
                        </li>
                        {% if app.user and is_granted('ROLE_ADMIN') %}
                            <li class="nav-item">
                                <span class="navbar-text me-3">|</span>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle fw-semibold" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                                    ⚙️ Administración
                                </a>
                                <ul class="dropdown-menu">
                                    <li><a class="dropdown-item" href="{{ path('app_user_index') }}">👥 Usuarios</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                    <li><a class="dropdown-item" href="{{ path('app_aplicaciones_index') }}">📱 Aplicaciones</a></li>
                                    <li><hr class="dropdown-divider"></li>
                                </ul>
                            </li>
                        {% endif %}
                    </ul>
                    <ul class="navbar-nav ms-auto">
                        {% if not app.user %}
                            <li class="nav-item">
                                <span class="navbar-text me-3">|</span>
                            </li>
                            <li class="nav-item me-4">
                                <a class="nav-link fw-semibold" href="{{ path('app_login') }}">🔑 Iniciar Sesión</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link fw-semibold" href="{{ path('app_user_new') }}">✨ Registrarse</a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <span class="navbar-text me-3">|</span>
                            </li>
                            <li class="nav-item me-3">
                                <span class="navbar-text fw-semibold">Hola, {{ app.user.nombre|default(app.user.userIdentifier) }}!</span>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link fw-semibold" href="{{ logout_path() }}">🚪 Cerrar Sesión</a>
                            </li>
                        {% endif %}
                    </ul>
                </div>
            </div>
        </nav>
        {% block body %}{% endblock %}
        <footer class="text-center mt-5 py-4" style="background: rgba(0, 0, 0, 0.8); border-top: 2px solid #00d4ff; color: #e0e0e0;">
            <div class="container">
                <p>&copy; 2026 Trabajo Juegos. Todos los derechos reservados. 🎮</p>
            </div>
        </footer>
    </body>
</html>